app:
  name: "auth-starterkit-be"
  version: "0.0.1"
  environment: "development"

  server:
    host: "localhost"
    port: "3010"
    cors: "{url},{url}"

  redis:
    host: "localhost"
    port: "6379"
    password: "{password}"

  mongodb:
    host: "localhost"
    port: "27017"
    username: "{username}"
    password: "{password}"
    database: ""

  postgresql:
    host: "localhost"
    port: "5432"
    username: "{username}"
    password: "{password}"
    database: ""

  minio:
    host: "localhost"
    port: "9000"
    access_key: "{access_key}"
    secret_key: "{secret_key}"

  rabbitmq:
    host: "localhost"
    port: "5672"
    username: "{username}"
    password: "{password}"

  jwt:
    public: ""
    private: ""

  discord:
    token: "{token}"